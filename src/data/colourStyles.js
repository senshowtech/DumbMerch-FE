export const colourStyles = {
  control: (styles) => ({
    ...styles,
    backgroundColor: "#181818",
  }),
  option: (styles) => ({
    ...styles,
    color: "black",
  }),
  input: (styles) => ({
    ...styles,
    color: "white",
  }),
  singleValue: (styles) => ({
    ...styles,
    color: "white",
  }),
};
